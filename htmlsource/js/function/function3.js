// 화살표 함수
const myFunc1 = () => {};

const showMessage = () => console.log("안녕하세요");

const multiply = (x, y) => console.log(`${x}, ${y}`);

const myFunc2 = (x, y) => x * y;

const fruits = ["사과", "망고", "바나나", "키위", "포도", "자두"];

fruits.forEach(function (element) {});
fruits.forEach((element) => console.log(element));
